/**
 * Camada de API:
 * ApiClient → AuthInterceptor → Request → Response → ErrorHandler
 *
 * Implementação na Fase de infraestrutura.
 */
export const apiClientPlaceholder = {
  name: "ApiClient",
} as const
